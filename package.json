{
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build src/index.ts --minify --splitting --outdir=out",
    "start": "bun run out/index.js",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.8.0",
    "@t3-oss/env-core": "^0.7.3",
    "hono": "^3.12.3",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/bun": "^1.0.0",
    "prisma": "^5.8.0"
  }
}
